language: node_js
before_install:
  - npm install -g npm@latest

node_js:
  - node
  - lts/*

jobs:
  include:
    - stage: release
      node_js: lts/*
      deploy:
        provider: script
        skip_cleanup: true
        script:
          - npx semantic-release
